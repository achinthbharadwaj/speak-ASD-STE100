name: cursor-skill in sync

# Fails if .cursor/skills/speak-ste/SKILL.md drifts from the canonical
# skills/speak-ste/SKILL.md. The .cursor copy is a real file, not a symlink,
# so Windows clones and GitHub ZIP downloads work.
on:
  pull_request:
    paths:
      - skills/speak-ste/SKILL.md
      - .cursor/skills/**
      - .github/workflows/cursor-skill-sync.yml
  push:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Fail if the .cursor copy differs from SKILL.md
        run: |
          cmp skills/speak-ste/SKILL.md .cursor/skills/speak-ste/SKILL.md || {
            echo "::error::.cursor copy is out of sync. Run: cp skills/speak-ste/SKILL.md .cursor/skills/speak-ste/SKILL.md"
            exit 1
          }
